### Issue
**环境**：Windows 10/11（待确认其他系统）  
**复现命令**：`sdt-greet --name "   "`  
**期望结果**：程序以非零退出码（如 2）结束，不输出问候语。  
**实际结果**：程序输出 `Hello, !`，并以 0 退出。  

---
### 提交信息
**标题**：fix: reject whitespace-only name with exit code 2  
**正文**：当 `--name` 仅包含空白字符时，当前行为是输出空问候并以 0 退出，不符合预期。现已增加检查，若 `name` 为空白则调用 `SystemExit(2)`，并添加对应测试用例。

---
### 评审意见
**Blocking**：`cli.py` 中 `main()` 未处理空白输入，导致静默失败。建议按上述提交修复，添加空白检查并抛出 `SystemExit(2)`。此缺陷影响程序健壮性，必须在合并前解决。
