name: Proselint
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install proselint
      - run: proselint check $(git ls-files '*.md' | grep -v 'week04/02/code-quality.md')
